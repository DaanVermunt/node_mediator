import { Policy } from '../MDP/process/policy'
import { State } from '../MDP/state/state'

function writePolicy(states: State[], policy: Policy) {
    // console.log(policyToString(states, policy))
}
